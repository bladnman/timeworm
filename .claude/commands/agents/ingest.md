Transform inbox files into timeline data

Read `open-agents/INSTRUCTIONS.md` for system context, then read `open-agents/agents/ingest.md` for the full agent specification.

Process the inbox to create timeline data:
$ARGUMENTS
